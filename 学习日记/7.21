1. 正则表达式
    功能：识别正则字符串。正则字符串是由一系列线性规则组成的字符串。
    方法：列一个步骤清单，具体描述正则字符串的结构。
    应用：识别地址。

2. tag.attrs
    功能：获取特性。
    方法：无需输入，返回由特性名和特性值组成的字典。

3. 匿名函数(functionName = lambda arg: expression)
    功能：临时声明、立刻执行、仅调用一次、效率高。
    方法：通过functionName调用，functionName(arg)

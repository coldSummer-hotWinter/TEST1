1. .encode(unicode):
    功能：编码。
    方法：输入编码，再用BeautifulSoup对象调用，返回字符串。

2. .decode(unicode):
    功能：解码。
    方法：输入解码，再用BeautifulSoup对象调用，返回字符串。

3. UnicodeDammit(Unicode):
    功能：检测未知编码。
    方法：输入未知编码，返回一个对象。

4. copy.copy(content):
    功能：复制对象，指定不同的内存地址。
    方法：输入被复制的内容，返回复制的内容。
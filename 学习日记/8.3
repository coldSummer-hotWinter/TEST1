1. .prettify(unicode):
    unicode：指定编码，默认为uft-8。
    功能：将HTML文档，按照标准进行缩进输出。
    方法：通过BeautifulSoup对象、tag对象调用，返回字符串。

2. .get():
    功能：提取元素的特性值。
    方法：通过一个元素对象的调用，返回元素的特性值。

3. .get_text(sep, strip):
    sep：分隔符，字符串形式。
    strip：去除两边空白，布尔值。
    功能：提取元素的文本内容。
    方法：通过一个元素对象调用，输入可选参数，返回元素的文本内容。

4. BeautifulSoup工作过程：
    输入一个字符串或文件句柄----->文档被转换成unicode----->用解析器来解析文档，将节点创建为python对象

5. attrs:
    功能：获取特性值
    方法：类似字典，可对内容进行添加、修改、删除

6. .select(tag_name):
    功能：提取所有符合的元素。可逐层寻找。
    方法：输入元素名、id值、class值、属性、属性的值，返回由符合元素组成的列表。